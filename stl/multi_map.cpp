// we can store duplicate keys here unlike map but in sorted order likewise

// Unordered map :- it wont store in sorted order and unique keys

// map : logn time
// unordered map : O(1) ..constant time (on an average)